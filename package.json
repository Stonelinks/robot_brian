{
  "name": "robot_brian",
  "description": "Robot Brian",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=8"
  },
  "scripts": {
    "gen_app_yaml": "node gen_app_yaml.js > app.yaml",
    "deploy": "npm run gen_app_yaml && gcloud app deploy",
    "start": "node src/main.js",
    "lint": "prettier --write --no-semi **/*.js"
  },
  "dependencies": {
    "botkit": "0.6.18",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "prettier": "1.12.1"
  }
}
